<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand fixed-color" href="/">DVM Mon-web</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <button id="theme-toggle" class="btn btn-secondary">Dark Mode</button>
            </li>
            <% if (user) { %>
                <li class="nav-item">
                    <button class="btn btn-primary" data-toggle="modal" data-target="#commandModal">Send Command</button>
                </li>
                <li class="nav-item">
                    <a class="btn btn-primary" href="/users">User Management</a>
                </li>
            <% } %>
            <% if (!user) { %>
                <li class="nav-item">
                    <a class="btn btn-primary" href="/login">Login</a>
                </li>
            <% } else { %>
                <li class="nav-item">
                    <a class="btn btn-secondary" href="/logout">Logout</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>